# Attributes

An attribute is a piece of data stored in a class instance.

```php
class Bicycle {
    public $colour;
};

$bike = new Bicycle();

$bike -> colour = 'blue';

echo $bike -> colour; // blue
```
