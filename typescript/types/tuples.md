# Tuples

Tuple in TS = array of a fixed length with fixed type.

This is more explicit

```typescript
let possibleResults: [number, number];

possibleResults = [1, 3]; // valid
possibleResults = [2, 5, 7]; // invalid
```

Here both the first and second values must be numbers, and there can only be two values
